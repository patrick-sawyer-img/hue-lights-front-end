import styled from "styled-components"

interface Props {
  changePage: () => void
}

export function Select({
  changePage,
}: Props) {
  return (
    <Wrapper>

    </Wrapper>
  )
}

const Wrapper = styled.div``